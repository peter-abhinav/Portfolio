---
import {Image} from "astro:assets"
import { cn } from "../../utils"
import Highlight from "./highlight.astro"
const {colorTitle, class: className,title="", Memoji, AltMemoji} = Astro.props
---

<!-- <section>
        <h1 class="bg-white text-[60px] flex-1 md:text-[30px] lg:text-[40px]">
          <strong class="text-accent">{colorTitle} </strong>{title}
        </h1> 
        
        <Image src={Memoji} alt={AltMemoji || "Memoji"} class="w-auto bg-green-900 md:h-[70px] lg:h-[90px]"/>


      </div>

<div class="w-full md:w-2/3 flex flex-col gap-4 text-[18px] md:text-[14px] lg:text-[18px]">
    <slot />
  </div>
-->

<section>
  <div class={cn(className,'flex flex-wrap md:flex-nowrap mb-20 items-start')}>
      <!-- Text/Image -->
      <div class="w-full md:w-1/3 flex flex-col items-center md:flex-row items-center">
        
        <h1 class="text-[60px] md:text-[30px] lg:text-[40px]"><Highlight>{colorTitle}</Highlight>{title}</h1>
        <Image src={Memoji} alt={AltMemoji || "Memoji"} class="w-auto md:h-[60px] lg:h-[90px]"/>
      </div>

      <!-- text/slot -->
      <div class="w-full md:w-2/3 flex flex-col md:text-[14px] lg:text-[20px]">
        <slot/>
      </div>
  </div>
</section>