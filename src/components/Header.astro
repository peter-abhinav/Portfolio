<!-- Header Section -->
<header class="mt-[18px] max-w-7xl mx-auto px-4 sm:px-6 md:px-8">
  <main class="py-2">
    <div class="w-full max-w-md md:max-w-2xl lg:max-w-3xl mx-auto px-6 md:px-8 py-4 flex items-center justify-between text-[var(--color-text-default)] rounded-2xl">

      <!-- Logo -->
      <a href="/" target="_self">
        <h1 class="text-2xl font-bold">ap</h1>
      </a>

      <!-- Status Badge -->
      <a href="mailto:abhinavpeter1999@gmail.com" target="_blank">
        <div
          class="relative flex items-center gap-2 rounded-[10px] border px-4 py-2 text-sm shadow-sm transition-all hover:shadow-md"
          style="border-color: var(--color-border-500); color: var(--color-text-default);"
        >
          <!-- Pulsating Dot -->
          <span class="relative w-[9px] h-[9px]">
            <span class="absolute inset-0 rounded-full bg-[#10B77F] z-10"></span>
            <span class="absolute inset-0 rounded-full border border-[#32C924] animate-pingRing z-0"></span>
          </span>

          <!-- Status Text -->
          <span class="font-semibold">Open to Work</span>
        </div>
      </a>
    </div>
  </main>
</header>
